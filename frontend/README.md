# DueClaim frontend

Vite + React + TypeScript + Tailwind v4.

```sh
cp .env.example .env   # set VITE_API_URL to the ApiUrl stack output
npm install
npm run dev
npm run build          # tsc + vite build -> dist/
```
